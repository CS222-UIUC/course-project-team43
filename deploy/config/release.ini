[app]
PrefixUrl = http://127.0.0.1:8000

[server]
#debug or release
RunMode = release
HttpPort = 8000
ReadTimeout = 60
WriteTimeout = 60
DownloadPath = /data/
ZapConfPath = /app/backend/conf/zap.json

[database]